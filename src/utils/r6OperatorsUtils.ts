import r6Operators from "r6operators";
import type { Operator } from "r6operators";

// export type TOperator = keyof typeof r6Operators;
// export const r6OperatorsList = Object.keys(r6Operators) as Array<TOperator>;

export const MAX_R6OPERATORS_ID = 69;
